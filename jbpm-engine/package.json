{
  "name": "@fabrixly/jbpm-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Node.js-native BPM engine + visual workflow builder, built on @fabrixly/bpmn-sdk",
  "workspaces": [
    "server",
    "app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server & npm run dev --workspace app",
    "dev:server": "npm run dev --workspace server",
    "dev:app": "npm run dev --workspace app",
    "build": "npm run build --workspace server && npm run build --workspace app",
    "test": "npm run test --workspace server",
    "typecheck": "npm run typecheck --workspace server"
  },
  "engines": {
    "node": ">=20"
  }
}
