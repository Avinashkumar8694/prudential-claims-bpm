// Sub-process — embedded/transaction/event. Nested-graph execution is a follow-up; passes through today.
import type { NodeHandler } from '../types.ts';
export const handler: NodeHandler = () => ({});
