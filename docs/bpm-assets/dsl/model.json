{
  "kind": "dsl",
  "model": {
    "entries": [
      {
        "scope": "when",
        "nl": "There is a claim over {amount}",
        "mapping": "$c : Claim( amount > {amount} )"
      },
      {
        "scope": "then",
        "nl": "Set claim status to {status}",
        "mapping": "modify( $c ) { setStatus( \"{status}\" ) };"
      }
    ]
  }
}
