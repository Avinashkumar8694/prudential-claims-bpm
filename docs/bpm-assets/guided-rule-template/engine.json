{
  "types": [
    {
      "name": "Claim",
      "fields": [
        { "name": "amount", "type": "double" },
        { "name": "region", "type": "string" },
        { "name": "tier", "type": "string" }
      ]
    }
  ],
  "guidedRuleTemplates": [
    {
      "name": "Tier pricing",
      "when": [
        { "fact": "Claim", "as": "c", "where": { "amount": { "gte": "{min}" }, "region": "{region}" } }
      ],
      "then": [
        { "set": "c", "fields": { "tier": "{tier}" } }
      ],
      "rows": [
        { "min": 5000,   "region": "US", "tier": "STANDARD" },
        { "min": 100000, "region": "US", "tier": "HIGH" }
      ]
    }
  ],

  "$note": "The WHOLE thing you write — ONE rule skeleton (same when/then as a DRL ruleset rule) whose values can be \"{param}\" placeholders, plus `rows` that fill them. Each row expands to one rule (type preserved: {min} -> the number). fromEngineProject generates the Business Central .template (TemplateModel XML — best-effort: well-formed, BC-load not verified). Runtime: expandTemplate substitutes the rows and the SAME rule engine runs the N rules. It's semantically a decision table, so a DMN/guided decision table (../dmn, ../guided-decision-table) or DRL ruleset (../drl) is the guaranteed-executable path. See scenarios.md §0."
}
