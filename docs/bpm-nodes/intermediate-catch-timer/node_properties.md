# Intermediate Catch Event (Timer) — properties

| Property | XML | Required | Notes |
|----------|-----|:--------:|-------|
| id / name | attrs | | e.g. `Timer_1mWait` |
| incoming / outgoing | elements | yes | one each |
| timeDuration | `timerEventDefinition/timeDuration` | one-of | `PT15M` back-off |
| timeCycle / timeDate | alternatives | | |

## Input / output mapping
None.
